# WikipediaSpeedrunBot
Creating a bot that can beat anyone in a Wikipedia speedrun

if you want to generate an exe of this tool run this command
```pyinstaller --onefile --hidden-import="selenium" --hidden-import="selenium.webdriver" --hidden-import="selenium.webdriver.chrome.webdriver" find_path.py```
make sure you have pyinstaller installed.